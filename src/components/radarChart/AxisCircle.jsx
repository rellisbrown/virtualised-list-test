const AxisCircle = ({ radius, filter, fill, stroke, fillOpacity }) => (
  <circle
    r={radius}
    filter={filter}
    fill={fill}
    stroke={stroke}
    fillOpacity={fillOpacity}
  />
);

export default AxisCircle;
